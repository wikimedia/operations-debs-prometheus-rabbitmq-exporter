description "prometheus-rabbitmq-exporter"
respawn
respawn limit 10 30

start on (local-filesystem and net-device-up IFACE!=lo)

setuid prometheus

script
    exec /usr/bin/prometheus-rabbitmq-exporter -c /etc/prometheus/rabbitmq-exporter.yaml
end script
        